[package]
name = "shared"
version.workspace = true
edition.workspace = true

[dependencies]
base = { git = "https://github.com/epimore/pigs.git",rev = "a81dea9a", features = ["net"] }
paste = "1.0"

# 共享依赖
utoipa.workspace = true
